apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: Subscription
      name: keycloak-operator
      namespace: keycloak
    patch: |-
      - op: replace
        path: /spec/channel
        value: fast
