apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
    - name: PATCH-ME
      type: HTPasswd
      mappingMethod: claim
      htpasswd:
        fileData:
          name: PATCH-ME
